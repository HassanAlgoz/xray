# We place __init__.py files:
# To make Python treat the containing directories as Python packages
# To execute initialization code when the package is imported
# To control what parts of the package are publicly accessible
